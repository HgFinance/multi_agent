# --- Core config & utils ---
python-dotenv
PyYAML
requests
httpx

# --- LLM providers (matches keys already in .env) ---
anthropic
openai

# --- Data / Quant (Research, Quant/Backtest departments) ---
pandas
numpy
scipy
jsonschema>=4.10,<5

# --- API & realtime (initial stack per HEDGE_FUND_MASTER_PLAN.md 13.1) ---
fastapi
uvicorn[standard]
websockets

# --- Contracts (trading/contracts.py) ---
# Agent와 결정론적 서비스의 경계에서 입력을 검증한다. fastapi가 끌어오지만
# 계약 모듈이 직접 의존하므로 명시한다.
pydantic>=2
# PAPER order orchestration exposes an authenticated FastMCP endpoint directly;
# do not rely on the separately installed Hermes CLI to provide this runtime.
mcp>=1.26,<2
# Internal service-token helpers use PyJWT. Browser/user login tokens are not
# implemented by the local mock portfolio stack.
PyJWT[crypto]>=2.8,<3

# --- Storage clients ---
psycopg2-binary
# asyncpg: governance/workforce 도메인 서비스의 비동기 Repository 드라이버
# (TEAM_YOUNGJU 9.1, TECH_STACK_DECISIONS 316행). F01/F19 asyncpg Repository 가 사용.
asyncpg
redis
pgvector

# --- Agentic RAG (skills/agentic-rag — evidence-qa-agent, hallucination-critic, compliance-policy-agent) ---
langgraph
# langgraph-checkpoint-postgres: TECH_STACK_DECISIONS.md 39행 "LangGraph Persistence" P0 필수.
# PostgresSaver가 psycopg(v3)를 쓴다 - 위 psycopg2-binary(Repository 직접 접근용)와는 별개 드라이버다.
# HITL Mandate 변경 Workflow(change_workflow.py)의 interrupt()/재개에 사용(2026-08-05).
langgraph-checkpoint-postgres

# --- Observability ---
opentelemetry-sdk
opentelemetry-exporter-otlp
prometheus-client
# langfuse: HR(07-agent-workforce)이 6개 투자본부 Worker의 유휴 여부를 관측하는 용도
# (2026-08-10, 영주 결정). langsmith 와 마찬가지로 orchestration/llm_observability.py 가
# lazy import 하므로 설치 안 해도 나머지 파이프라인은 그대로 돈다 - LANGFUSE_TRACING=false
# (기본값)일 때 이 패키지가 없어도 에러가 나지 않는다. 설치 확인 버전: langfuse==4.14.3.
langfuse

# --- Test (Risk/QA 자체 점검 pytest 전환분, departments/03-risk, 06-ai-qa-audit) ---
pytest

# NOTE: The Hermes agent runtime itself (NousResearch/hermes-agent) is a separate
# CLI/orchestrator that these department .yaml files target — it is not a PyPI
# package and is not listed here; install it per its own repo instructions.
