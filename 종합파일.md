# 프론트엔드–백엔드 직접 E2E 연동 종합 감사·보완 명세

**작성일:** 2026-08-05  
**대상:** `/Users/baiohelseu/Desktop/Project/multi_agent` 현재 작업 트리  
**범위:** `ai-office` 프론트엔드, `apps/api` BFF, Governance API 프록시, `PortfolioRuntime`, LangGraph 부서 파이프라인, Supabase·Ollama·Risk·QA 연결  
**목적:** 화면 입력이 백엔드의 실제 파이프라인으로 전달되고, 결과·실패·보류·근거가 같은 실행 단위로 다시 화면에 도착하는지 점검하고, 운영 가능한 E2E 완성 조건을 고정한다.

> 이 문서는 “절대 문제가 생기지 않는다”는 보증서가 아니다. 금융·분산 시스템에서는 장애를 0으로 보장할 수 없다. 대신 문제가 발생해도 주문 확대나 자동 승인이 일어나지 않고, `HOLD/REJECT/DENY/ESCALATE`로 멈추며 원인을 재현할 수 있게 하는 **fail-closed acceptance 기준**을 정의한다. 현재 구현은 주문·원장 변경이 없는 advisory prototype이므로, 아래 기준을 충족하기 전에는 실거래 시스템으로 표현하거나 운영 승격하지 않는다.

## 0. 결론 요약

현재 직접 연결의 뼈대는 존재한다.

```text
브라우저 입력
  → ai-office/app/ops/portfolioClient.ts
  → FastAPI BFF /ui/portfolio-recommendations
  → apps/api/portfolio_runtime.py (process-local run)
  → orchestration/workflows/portfolio_recommendation.py
  → 부서별 LangGraph Worker fan-out/fan-in
  → Supabase PIT 데이터·Risk·QA 게이트
  → 결과/이벤트를 PortfolioRuntime에 투영
  → GET /ui/snapshot + WS /ws/operations
  → BffProvider → PortfolioInterviewPanel
```

Mandate Governance는 별도 경로다.

```text
Mandate 화면
  → /ui/mandates/{mandate_id}/change-requests
  → Governance API의 Risk·QA·사용자 승인 흐름
  → mandate version
```

따라서 현재 상태는 다음과 같다.

| 영역 | 현재 판단 | 핵심 근거 |
|---|---|---|
| 프론트엔드 → BFF | 연결됨 | `portfolioClient.ts`가 universes/start/status/approval API를 호출한다. 다만 실제 화면의 주 상태 갱신은 전역 `/ui/snapshot`에 의존한다. |
| BFF → LangGraph | 연결됨 | `PortfolioRuntime`이 백그라운드에서 `run_portfolio_recommendation_pipeline_async`를 호출한다. 실행은 process-local이다. |
| LangGraph → 부서 Worker | 연결됨 | research → trading → risk → qa → accounting → ceo 및 부서별 fan-out/fan-in이 있다. 실패는 안전 상태로 전파된다. |
| LangGraph → Supabase | 읽기 연결 | 연결된 DB에서 research 문서가 읽히는 것은 확인되지만, 실행 당시 포트폴리오 후보·PIT 종목·시장 스냅샷이 없으면 안전하게 HOLD가 된다. |
| Governance → 추천 실행 | **완전한 단일 계약 아님** | 화면은 Governance에 전체 정책을 제출하지만 추천 POST에는 기본 프로필만 보낸다. `mandate_version_id`로 두 실행을 묶지 않는다. |
| 추천 실행 → 화면 결과 | 부분 연결 | `run_id` 상태 API가 있지만 화면은 반환된 `run_id`를 저장·조회하지 않고 전역 `/ui/snapshot`을 새로 읽는다. |
| 실제 브라우저 E2E | **미완성** | 저장소에는 FastAPI·Python 계약 테스트와 rendered HTML 테스트는 있으나, 실제 브라우저 → BFF → WebSocket → 화면 상태를 검증하는 Playwright/Cypress 테스트가 없다. |
| 실거래·원장 쓰기 | 의도적으로 미연결 | `external_writes=false`, `/approval`도 주문·원장 변경을 수행하지 않는 자문 결과 승인이다. |

실행 가능성의 현행 추정치는 코드 커버리지가 아니라 Acceptance 기준이다.

| 범위 | 현재 추정 | 80%까지 가기 위해 필요한 것 |
|---|---:|---|
| 프론트 입력·BFF 호출 | 60% | 실행별 상태 구독, 런타임 DTO 검증, 전체 정책/버전 전달 |
| BFF·런타임·LangGraph | 65% | 영속 run store, idempotency, readiness, 실행별 event replay |
| Risk·QA 내부 Worker/게이트 | 80~85% / 75~80% | 실제 정책 Corpus·PIT 데이터·운영 trace 연결 |
| 프론트↔백엔드 공동 E2E | 40~50% | 브라우저 E2E와 공유 계약·인증·실패 매트릭스 |
| 실거래 운영 준비도 | 20% 미만 | 현재 외부 쓰기·브로커·영속 운영 인프라가 범위 밖이므로 별도 Gate 필요 |

## 1. 현재 E2E 계약과 소유권

### 1.1 책임 경계

| 계층 | 소유 | 허용 책임 | 금지 책임 |
|---|---|---|---|
| 브라우저 | `ai-office` | 입력, 사용자 확인, 상태·근거·보류 표시 | DB 직접 접근, Supabase Service Role, Broker credential, Risk 계산 |
| BFF | `apps/api` | 인증 후 DTO 변환, 읽기 projection, 추천 실행 시작, Governance proxy | LLM 결과를 무검증 주문으로 변환, 브라우저 정책을 최종 권한으로 신뢰 |
| Runtime | `apps/api/portfolio_runtime.py` | 실행 ID, 상태, 이벤트, 결과 projection | 프로세스 재시작 후에도 영속 상태를 보장한다고 주장 |
| Orchestration | `orchestration/workflows/portfolio_recommendation.py` | 부서 순서, handoff, fan-out/fan-in, 실패 전파, HOLD | 실패한 부서를 성공으로 합산, 자동 승인 |
| Risk | `departments/03-risk` | 결정론적 한도·자산군·섹터·pre-trade 검증 | LLM 출력만으로 binding 승인 |
| QA | `departments/06-ai-qa-audit`, `skills/agentic-rag` | evidence·hallucination·permission·incident 검증 | 근거 없는 결론을 운영 승인으로 승격 |
| Governance | CEO Office/Governance API | Mandate version, Risk·QA·사용자 승인 상태 | 주문 제출·원장 수정·NAV 확정 |
| Supabase | DB | PIT 후보·시장·research·회계 read model | 브라우저가 직접 사용하는 비밀 저장소 |
| Broker/OMS/Ledger | 별도 운영 계층 | 향후 승인된 OrderIntent의 결정론적 처리 | 현재 추천 API에서 직접 호출 |

### 1.2 반드시 유지할 의미 구분

다음 객체를 같은 것으로 취급하면 E2E가 안전하지 않다.

```text
Agent Decision ≠ Strategy Signal ≠ OrderIntent ≠ Order ≠ Fill ≠ Ledger Posting
```

이번 범위의 추천 API는 첫 세 단계 중 `advisory recommendation`까지만 다룬다. `/ui/portfolio-recommendations/{run_id}/approval`의 `APPROVE`는 **추천 결과에 대한 사용자 확인**이지 주문 승인, 브로커 전송, 원장 반영이 아니다. UI와 문서 모두 이 의미를 유지해야 한다.

## 2. 프론트엔드에서 해야 할 내용

대상 파일: `ai-office/app/ops/PortfolioInterviewPanel.tsx`, `ai-office/app/ops/portfolioClient.ts`, `ai-office/app/ops/bffClient.tsx`, `ai-office/app/ops/readModel.ts`, `ai-office/app/ops/governanceClient.ts`.

### 2.1 현재 구현되어 있는 것

1. 브라우저는 BFF `NEXT_PUBLIC_BFF_URL`만 호출하고 직접 DB/credential을 사용하지 않는다.
2. `GET /ui/portfolio-universes`로 백엔드 소유 유니버스를 받아 국내 주식 watchlist를 표시한다.
3. `POST /ui/portfolio-recommendations`를 통해 실행을 시작하고, 추천 결과 승인 API를 호출할 수 있다.
4. BFF 상태는 REST polling과 `WS /ws/operations`로 갱신한다.
5. WebSocket sequence가 이전 값보다 작거나 알 수 없으면 화면 상태를 되돌리지 않고 REST snapshot을 다시 요청한다.
6. BFF 연결이 `connected`가 아니면 Governance 명령을 막고, runtime 상태와 `HOLD`·PIT warning을 표시한다.
7. 국내 주식이 아닌 asset class를 화면 추천 목록에서 제외한다.
8. Mandate 초안을 localStorage에 저장하고, Governance API에 정책·목표·trace ID를 제출한다.

### 2.2 부족한 부분과 위험

#### F-01. 컴파일 타입과 런타임 계약이 다르다 — P0

`portfolioClient.ts`의 universes/start/approval 응답은 `body as ...` 형태의 unsafe cast다. `PortfolioInterviewPanel.tsx`의 `RuntimeResult`·`DepartmentReport`도 백엔드 응답을 독립적으로 느슨하게 선언한다. TypeScript가 빌드되더라도 실제 JSON이 바뀌면 화면이 `undefined`를 정상 상태처럼 표현할 수 있다.

**보완:** BFF OpenAPI 또는 `apps/api/portfolio_schemas.py`를 단일 원천으로 두고 TypeScript client/DTO를 생성한다. 생성이 어려운 동안에는 `zod` 같은 런타임 parser를 추가해 모든 `fetch` 응답을 검증하고, 실패 시 `CONTRACT_ERROR → HOLD/수동 검토`로 표시한다. `result`, `department_reports`, `worker_reports`, `pipeline_events`, `data_context`의 nested `dict[str, Any]`도 단계적으로 Pydantic 모델로 좁힌다.

#### F-02. 실행별 상태가 아니라 전역 snapshot을 읽는다 — P0

`startRecommendation()`은 POST 응답의 `run_id`를 버리고 `refresh()`를 호출한다. 실제 실행 결과는 `GET /ui/portfolio-recommendations/{run_id}`로 조회할 수 있지만 현재 화면은 전역 `/ui/snapshot`의 `operations.runtime.result`를 읽는다. 다른 탭·재시작·동시 요청·이전 실행이 섞일 수 있다.

**보완:** POST의 `run_id`를 상태에 저장하고, 해당 run의 status endpoint를 primary source로 사용한다. WebSocket은 이벤트 알림으로만 사용하고 `run_id`·sequence를 검증한 뒤 해당 run을 재조회한다. `/ui/snapshot`은 사무실 전체 dashboard projection으로 유지하되 portfolio interview의 실행 결과 source로 사용하지 않는다.

#### F-03. Governance 정책과 추천 실행이 하나의 version으로 묶이지 않는다 — P0

화면은 `buildMandatePolicy(input)`으로 전체 정책을 Governance API에 제출한다. 반면 추천 POST의 `recommendationInput()`에는 user/profile 기본값과 `include_stock`, `include_derivatives`, `query`만 전달된다. `max_sector_weight`, `max_gross_exposure`, `max_daily_loss`, `preferred_sectors`, `excluded_sectors`, `allowed_asset_classes`, `forbidden_asset_classes` 및 확정된 `mandate_version_id`가 추천 실행 계약에 없다.

**보완:** Governance가 `FAST_APPLIED` 또는 `ACTIVATED`를 반환할 때 `mandate_version_id`와 `policy_hash`를 발급하고, 추천 POST는 이를 필수로 받거나 BFF가 서버에서 현재 활성 version을 조회해 주입한다. 백엔드는 “브라우저가 localStorage에 보관한 정책”을 최종 정책으로 신뢰하지 않는다. 실행 결과에도 `mandate_version_id`, `policy_hash`, `input_hash`를 표시한다.

#### F-04. 실제 브라우저 E2E가 없다 — P0

현재 `ai-office/tests/rendered-html.test.mjs`는 렌더링 결과 문자열을 검사하는 정적 테스트다. 실제 Chromium에서 폼 입력, fetch, WebSocket, reconnect, 결과 표시, 실패 표시를 검증하지 않는다.

**보완:** Playwright를 추가하고 최소 다음 경로를 실제 브라우저로 실행한다.

- universes 조회 → Mandate 입력 → Governance fast path → recommendation POST → run status polling
- WebSocket 정상 이벤트 → `run_id` status 재조회 → 부서별 worker/event 표시
- WebSocket 끊김·sequence gap → REST 재동기화 → stale/offline 표시
- PIT 데이터 없음 → 후보 0개·Risk reject·QA warn·HOLD 표시
- invalid 422, duplicate 409, Governance 503, worker failure/DEGRADED 표시
- 추천 `APPROVE`가 주문·원장을 변경하지 않는다는 UI 상태 표시

#### F-05. 고급 입력의 저장과 실행 전달이 다르다 — P0

화면에서 보이는 한도는 Governance 제출에는 들어가지만 recommendation request에는 빠진다. 사용자는 “이 한도로 분석된다”고 이해할 수 있으나 실행 파이프라인에서는 다른 입력이 된다.

**보완:** 화면의 `start` 버튼은 `mandate_version_id` 없는 실행을 막는다. 초안만 있는 경우 `SAVE_ONLY`와 `RUN_ANALYSIS`를 분리하고, 실행 버튼에는 활성 version·정책 hash를 표시한다.

#### F-06. localStorage는 복구용 초안이지 신뢰 경계가 아니다 — P1

`startSavedPortfolioRecommendation()`은 localStorage를 읽어 기본값을 채운다. 사용자가 값을 바꿀 수 있고, 오래된 초안·다른 계정의 초안이 섞일 수 있다.

**보완:** localStorage에는 UI 복구용 draft만 두고 user/session에 귀속한다. 서버에서 현재 Mandate를 재조회해 hash/version을 비교하고 mismatch이면 실행하지 않고 다시 확인받는다.

#### F-07. BFF 연결 상태와 Governance 연결 상태가 완전히 같은 상태 머신이 아니다 — P1

현재 화면은 일반 BFF feed의 `connected`를 명령 가능 조건으로 사용한다. Governance API가 별도로 내려갔거나 portfolio runtime이 준비되지 않은 경우에도 동일한 버튼·오류 경험이 나오기 쉽다.

**보완:** `/health/ready` 결과에서 `bff`, `governance`, `supabase`, `ollama`, `pipeline`을 개별 상태로 표시한다. commandability는 `BFF connected ∧ Governance ready ∧ current user authorized`로 결정한다.

#### F-08. 숫자·상태 의미를 화면에서 재해석할 위험 — P1

`max_drawdown_pct`는 `0.10 = 10%` 비율이고 `10`은 허용되지 않는다. Decimal 금액·weight를 화면에서 JS `number`로 바꾸면 반올림·locale 문제를 만들 수 있다. 또한 `SKIPPED`, `SKIPPED_SAFE`, `NOT_REQUESTED`, `LIVE_DATA_NOT_READY`, `BLOCKED`, `DEGRADED`를 한 문구로 합치면 운영자가 원인을 알 수 없다.

**보완:** 금액·비율은 전송 시 문자열, 표시 시 명시적 formatter로 처리한다. 상태 enum과 `skip_reason`을 그대로 렌더링하고, `completed`, `skipped_safe`, `not_requested`, `failed`를 분리한다.

### 2.3 프론트엔드 완료 수용 기준

- [ ] 모든 BFF response가 런타임 schema parse를 통과해야 화면 상태에 반영된다.
- [ ] 추천 시작 후 `run_id`가 URL/state/local durable store 중 한 곳에 보존되고, 화면은 해당 run status만 읽는다.
- [ ] `mandate_version_id`, `policy_hash`, `input_hash`, `trace_id`, `case_id`가 실행 화면과 오류 화면에서 조회 가능하다.
- [ ] `APPROVE`는 advisory result 승인으로만 명시되고 주문·Fill·Ledger UI로 이어지지 않는다.
- [ ] WebSocket gap/reconnect 후 REST 재동기화가 완료될 때까지 stale 상태를 표시한다.
- [ ] 실제 브라우저 E2E가 CI에서 수행된다.
- [ ] 브라우저가 잘못된 응답·빈 후보·PIT 누락·Governance 장애를 성공으로 표시하지 않는다.

## 3. 백엔드에서 해야 할 내용

대상 파일: `apps/api/main.py`, `apps/api/portfolio_runtime.py`, `apps/api/portfolio_schemas.py`, `apps/api/portfolio_universe.py`, `orchestration/workflows/portfolio_recommendation.py`, `departments/03-risk`, `departments/06-ai-qa-audit`, Governance API.

### 3.1 현재 구현되어 있는 것

1. FastAPI BFF에 universe/start/status/approval API가 있다.
2. Pydantic response model이 기본적으로 `extra="forbid"`이고 start/status envelope가 고정되어 있다.
3. portfolio pipeline은 비동기 fan-out/fan-in이고 부서 handoff를 통해 research → trading → risk → qa → accounting → ceo 흐름을 가진다.
4. 선택되지 않은 Worker와 데이터 부족으로 차단된 Worker를 구분하는 방향으로 구현되어 있다.
5. Worker 시작·완료·차단 이벤트, payload/input hash, replay metadata를 runtime projection에 넣는다.
6. Supabase preflight/읽기 경로가 실패하거나 PIT 후보가 없으면 자동 승인하지 않고 `DEGRADED/HOLD`로 종료한다.
7. Risk와 QA는 각각 독립 Worker/계약/Trace 경계를 갖고, LLM이 최종 한도·인용·상태 전이를 결정하지 않도록 분리되어 있다.
8. 외부 쓰기는 기본적으로 false이며, 추천 승인 API가 broker·OMS·ledger를 호출하지 않는다.

### 3.2 부족한 부분과 위험

#### B-01. `PortfolioRuntime`이 process-local이다 — P0

`apps/api/portfolio_runtime.py`의 `_job`, `_tasks`, thread 기반 background run은 한 프로세스의 메모리에만 존재한다. BFF 재시작·다중 worker·컨테이너 교체 시 run status, event, result를 잃는다. 동시에 하나의 portfolio run만 허용한다.

**보완:** run/request/event/result를 영속 저장하는 execution store를 도입한다. 최소 필드:

```text
run_id, trace_id, case_id, user_id, mandate_version_id, policy_hash,
input_hash, workflow_version, model_version, status, phase,
started_at, updated_at, heartbeat_at, finished_at, error,
result_ref, event_sequence, idempotency_key
```

worker는 queue/task runner에서 실행하고, API는 status 조회·retry·cancel을 영속 store에 대해 수행한다. 이 단계 전에는 “운영에서 실행 이력을 보장한다”고 말하지 않는다.

#### B-02. 시작 idempotency·사용자 권한·run 소유권이 없다 — P0

추천 start API는 현재 process-local 실행을 시작한다. 동일 요청 재전송, 브라우저 refresh, 네트워크 timeout 후 재시도 시 중복 run이 생기거나 다른 사용자의 run ID를 조회할 수 있는 인증 경계가 명확하지 않다.

**보완:** `Idempotency-Key`를 필수로 하고 `(user_id, mandate_version_id, idempotency_key)` uniqueness를 둔다. status/approval은 인증된 주체가 run owner 또는 승인된 operator인지 검사한다. `user_id` request body만으로 권한을 결정하지 않는다.

#### B-03. nested response가 `dict[str, Any]`다 — P0

`PortfolioRecommendationResult`의 `suitability`, `task_plan`, `risk_gate`, `qa_gate`, `department_reports`, `worker_reports`, `data_context`, `replay`, `pipeline_events`가 넓은 dictionary다. top-level `extra="forbid"`만으로는 실제 E2E 계약이 고정되지 않는다.

**보완:** `DataQuality`, `RiskGate`, `QaGate`, `WorkerReport`, `DepartmentReport`, `PipelineEvent`, `ReplayMetadata`, `SuitabilityResult`를 Pydantic 모델과 enum으로 분리한다. `status`, `skip_reason`, `completed`, `skipped_safe`, `not_requested`, `failed`를 필수·상호검증한다. OpenAPI contract test에서 실제 fixture 전체를 response model에 통과시킨다.

#### B-04. Supabase 연결 성공과 추천 가능 데이터 준비는 별개다 — P0

현재 연결 결과처럼 DB preflight가 `PASS`여도 포트폴리오 버전·PIT 국내 equity instrument·market snapshot이 0이면 후보가 없다. research documents가 LIVE로 읽혔다는 사실만으로 가격·유니버스·valuation 근거가 생기지 않는다.

**보완:** 데이터 readiness를 별도 Gate로 유지한다.

```text
DB connectivity PASS
  ≠ schema PASS
  ≠ PIT universe ready
  ≠ market snapshots ready
  ≠ recommendation candidates ready
```

`candidate_count`, `domestic_instrument_count`, `market_snapshot_count`, as-of freshness, source/version hash를 요구하고, 실패 시 `LIVE_DATA_NOT_READY`와 `HOLD`를 반환한다. 실제 종목 유니버스·market snapshot 적재 job과 운영 alarm이 별도로 필요하다.

#### B-05. WebSocket은 실행 이벤트 스트림이 아니라 global operations feed다 — P1

`/ws/operations`는 operations snapshot/heartbeat/status event를 전달하고, frontend는 이를 보고 REST snapshot을 다시 요청한다. portfolio run별 replay cursor, 인증, backpressure, event persistence가 없다.

**보완:** `WS /ui/portfolio-recommendations/{run_id}/events?after=sequence` 또는 동등한 SSE를 추가한다. 이벤트는 `run_id`, `event_id`, monotonic `sequence`, `event_type`, `occurred_at`, payload schema, terminal 여부를 포함한다. 연결 직후 `after` cursor로 누락 이벤트를 재생할 수 있어야 한다.

#### B-06. worker thread의 timeout·cancellation·heartbeat가 불충분하다 — P1

background thread와 `asyncio.run`은 프로세스가 살아 있는 동안의 prototype에는 충분하지만, Ollama hang, Supabase slow query, Worker partial failure를 운영적으로 취소·재시도·복구하기 어렵다.

**보완:** 단계/Worker별 timeout, retry budget, heartbeat, cancellation token, terminal transition을 명시한다. retry가 중복 분석이나 중복 쓰기를 만들지 않도록 모든 단계는 read-only/idempotent여야 하며, terminal state는 `COMPLETED`, `DEGRADED`, `HOLD`, `FAILED`, `CANCELLED` 중 하나여야 한다.

#### B-07. readiness·security 경계가 개발용이다 — P0

CORS는 localhost 및 고정 개발 포트 정규식이다. 인증·CSRF·rate limit·body limit·production origin allowlist·run owner authorization이 이 BFF 계약에 포함되어 있지 않다. Governance API proxy도 BFF 내부에서 URL/timeout만 설정한다.

**보완:** production에서는 reverse proxy 또는 same-origin을 사용하고, 명시적 allowlist·세션/JWT 검증·CSRF 방어·request size/rate limit·audit actor를 적용한다. `/health/live`와 `/health/ready`를 나누고 Supabase/Governance/Ollama/queue/schema readiness를 노출하되 secret은 절대 반환하지 않는다.

#### B-08. BFF response의 오류/상태 코드 규칙을 더 고정해야 한다 — P1

현재 start는 202, 중복 실행은 409, universe 없음은 422, Governance 연결 오류는 503 방향이다. 그러나 프론트가 재시도 가능한 오류와 재시도하면 안 되는 validation/permission 오류를 일관되게 구분할 공통 error schema가 필요하다.

**보완:** 모든 오류에 `error_code`, `message`, `retryable`, `trace_id`, `run_id?`, `field_errors?`를 둔다. HTTP status만 해석하지 말고 frontend가 `retryable`과 안전 상태를 사용하게 한다.

#### B-09. BFF 입력 enum과 Governance 정책의 정합성 — P1

`mindset`, `experience`가 BFF request에서 plain string이면 허용되지 않은 값도 API 계층을 통과할 수 있다. `max_drawdown_pct`는 `0.10=10%` ratio로 문서화되어 있지만, 모든 소비자가 같은 단위를 사용하는지 contract test가 필요하다.

**보완:** BFF에서 Literal/Enum을 사용하고 `max_drawdown_pct`·weight·amount의 단위와 Decimal serialization을 통일한다. `max_daily_loss ≤ max_drawdown_pct`, asset class, sector, position limit의 정책 검증은 Risk Engine의 canonical policy를 통과해야 한다.

#### B-10. 실제 Ollama와 deterministic test의 차이가 있다 — P1

기본 worker runtime은 `deterministic_test`이고, Ollama는 환경 변수가 설정되어야 한다. Ollama 실패 시 `DEGRADED/HOLD`가 맞지만, 실제 모델·prompt·timeout·token budget·model version이 결과와 replay metadata에 포함되어야 한다.

**보완:** `PORTFOLIO_WORKER_RUNTIME=ollama`, `OLLAMA_BASE_URL`, `OLLAMA_CHAT_MODEL`를 readiness에 반영한다. TEST fixture와 LIVE/Ollama를 같은 계약 fixture로 비교하고, 모델 호출이 없었던 경우 confidence를 성공처럼 표시하지 않는다.

#### B-11. 외부 쓰기는 현재 연결되지 않은 것이 정상이나, E2E 범위를 오해하면 안 된다 — P0 문서/권한 Gate

현재 `external_writes=false`는 안전한 기본값이다. 이는 “실행까지 E2E 완료”가 아니라 “추천·표시까지만 E2E”라는 뜻이다. 실제 Broker/OMS/Ledger를 연결할 때는 별도 OrderIntent→Risk Engine→OMS→Fill→Ledger acceptance를 추가해야 한다.

### 3.3 백엔드 완료 수용 기준

- [ ] run이 API 프로세스 재시작과 다중 instance에서도 조회 가능하다.
- [ ] idempotency와 인증된 user/run 소유권 검사가 있다.
- [ ] 모든 nested result/event/report가 strict schema와 enum을 사용한다.
- [ ] `mandate_version_id`·`policy_hash`·`input_hash`가 pipeline input과 결과에 동일하게 남는다.
- [ ] PIT readiness 실패가 성공·빈 추천으로 변환되지 않고 `LIVE_DATA_NOT_READY/HOLD`가 된다.
- [ ] run-scoped event replay와 sequence gap 복구가 가능하다.
- [ ] Worker timeout/retry/cancel/heartbeat와 terminal state가 구현된다.
- [ ] production CORS/auth/CSRF/rate limit/readiness가 설정으로 강제된다.
- [ ] 외부 쓰기는 별도 승인 Gate 없이 절대 발생하지 않는다.

## 4. 백엔드와 프론트엔드가 함께 해야 할 내용

### 4.1 단일 E2E correlation 계약 — P0

아래 식별자는 한 요청에서 끝까지 보존해야 한다.

| 필드 | 생성 주체 | 용도 |
|---|---|---|
| `trace_id` | 브라우저/BFF | 한 사용자 요청의 전체 trace |
| `case_id` | Governance | Mandate change/approval case |
| `mandate_version_id` | Governance | 분석에 사용한 확정 정책 |
| `policy_hash` | BFF/Governance | 프론트 초안과 서버 정책 일치 확인 |
| `run_id` | BFF Runtime | 추천 파이프라인 실행 단위 |
| `input_hash` | BFF/pipeline | 실제 Worker 입력 snapshot 재현 |
| `event_id`/`sequence` | Runtime/event store | 이벤트 중복·누락·재생 방지 |
| `workflow_version` | Orchestration | graph 계약 변경 구분 |
| `model_version`/`prompt_version` | Worker runtime | Ollama/LLM replay |

최소 연결은 다음이어야 한다.

```text
trace_id
 ├─ case_id → mandate_version_id → policy_hash
 └─ run_id → input_hash → workflow/model/prompt version
                         └─ event sequence → Risk/QA reports → UI result
```

### 4.2 공유 상태 머신 — P0

프론트와 백엔드가 아래 상태를 같은 의미로 사용한다.

| 상태 | 의미 | UI 행동 | 자동 진행 |
|---|---|---|---|
| `QUEUED` | 실행 대기 | 대기 표시 | 가능 |
| `RUNNING` | Worker/부서 실행 중 | 진행/이벤트 표시 | 가능 |
| `WAITING_APPROVAL` | 명시적 사용자 승인 대기 | 승인·거절 표시 | 금지 |
| `COMPLETED` | 계약·게이트·데이터 조건을 모두 충족 | 결과 표시 | 주문 아님 |
| `DEGRADED` | 일부 실패/품질 저하 | 경고·수동 검토 | 금지 |
| `HOLD` | 안전 보류 | 사유·근거 표시 | 금지 |
| `BLOCKED` | 정책/권한/데이터 Gate 차단 | 차단 사유 표시 | 금지 |
| `FAILED` | 실행 자체 실패 | 재시도/문의 | 자동 승인 금지 |
| `CANCELLED` | 취소됨 | 종료 표시 | 금지 |

Worker/부서 보고서도 다음을 별도 필드로 가진다.

```json
{
  "status": "SKIPPED_SAFE",
  "skip_reason": "LIVE_DATA_NOT_READY",
  "completed": 0,
  "skipped_safe": 4,
  "not_requested": 0,
  "failed": 0,
  "technology": {
    "runtime": "deterministic_test",
    "framework": "LangGraph",
    "model": "qwen3:1.7b",
    "execution_mode": "non_binding"
  }
}
```

`SKIPPED_SAFE`는 “라우팅되지 않음”과 “데이터 부족으로 안전 차단됨”을 같이 의미하면 안 된다. 전자는 `NOT_REQUESTED`, 후자는 `LIVE_DATA_NOT_READY`다.

### 4.3 API 계약 표

| Method | Path | 현재 용도 | 읽기/쓰기 | 현재 위험/보완 |
|---|---|---|---|---|
| GET | `/health` | BFF liveness 성격 | 읽기 | dependency readiness와 분리 필요 |
| GET | `/ui/snapshot` | 사무실 전체 read model, DEMO/PAPER projection | 읽기 | portfolio run source로 사용하지 말고 dashboard 전용으로 유지 |
| WS | `/ws/operations` | global operations event/heartbeat | 읽기 | run-scoped replay·인증·cursor 필요 |
| GET | `/ui/portfolio-universes` | 백엔드 소유 유니버스 | 읽기 | TEST static catalog와 LIVE Supabase를 명확히 표시 |
| POST | `/ui/portfolio-recommendations` | advisory pipeline 시작 | process-local 상태 생성 | 인증·idempotency·mandate version 필수화 |
| GET | `/ui/portfolio-recommendations/{run_id}` | 실행 상태·result 조회 | 읽기 | 프론트의 primary source로 사용 |
| POST | `/ui/portfolio-recommendations/{run_id}/approval` | advisory 결과 승인/거절 | process-local 상태 변경 | 사용자 권한·version match·명시적 no-write 유지 |
| POST | `/ui/mandates/{mandate_id}/change-requests` | Governance proxy | 외부 Governance 요청 | 추천 run과 `case_id/version` 연결 필요 |
| GET | `/ui/mandates/{mandate_id}/current` | 활성 정책 조회 | 읽기 | 화면 localStorage와 hash 비교 |
| POST | `/ui/mandate-cases/{case_id}/advance` | Risk/QA/User 승인 단계 진행 | Governance 상태 변경 | actor 권한·재시도 idempotency 필요 |
| GET | `/ui/mandate-cases/{case_id}/timeline` | 승인/audit timeline | 읽기 | run timeline과 cross-link 필요 |

### 4.4 하나의 정식 E2E 시나리오

1. 프론트가 BFF readiness와 현재 Mandate를 읽는다.
2. 유효한 선택형 입력과 자연어 보조 입력을 만들고, `trace_id`를 생성한다.
3. 프론트는 서버에 Mandate change request를 보낸다. 서버는 정책 결정론 검증을 수행한다.
4. Review가 필요한 경우 Risk·QA·User 승인이 끝나기 전에는 recommendation run을 시작하지 않는다.
5. Governance가 `FAST_APPLIED/ACTIVATED`, `case_id`, `mandate_version_id`, `policy_hash`를 반환한다.
6. 프론트 또는 BFF가 같은 version을 포함한 recommendation start를 보낸다.
7. BFF는 `run_id`와 `input_hash`를 영속화하고 202를 반환한다.
8. 프론트는 run-scoped status endpoint를 즉시 조회한다.
9. WebSocket/SSE event가 오면 payload를 직접 믿지 않고 sequence와 run ID를 검증한 뒤 status를 재조회한다.
10. pipeline은 각 부서 내부 Worker를 fan-out하고 fan-in한다. 한 Worker가 실패해도 다른 Worker 결과를 자동 승인으로 간주하지 않는다.
11. Research/PIT data → Risk deterministic gate → QA evidence/hallucination/permission gate → CEO summary 순으로 handoff한다.
12. 후보·시장 스냅샷·정책·근거가 부족하면 `LIVE_DATA_NOT_READY/HOLD` 또는 `DEGRADED/HOLD`를 terminal result로 만든다.
13. 프론트는 completed/skipped/failed/skip_reason/technology/evidence를 표시한다.
14. `APPROVE`는 현재 advisory 결과에 대한 사용자 확인으로 저장하고, 외부 write count가 0인지 audit에서 확인한다.
15. 모든 결과는 `trace_id`, `case_id`, `run_id`, `mandate_version_id`, hash와 함께 replay할 수 있어야 한다.

### 4.5 E2E 실패 매트릭스

| 시나리오 | 백엔드 기대 | 프론트 기대 | 통과 조건 |
|---|---|---|---|
| 잘못된 enum/비율 | 422 typed error | 필드 오류 표시 | run 생성 안 됨 |
| 유니버스 없음 | 422 | 선택값 복구 | run 생성 안 됨 |
| Governance API down | 503 retryable=false/정책 정의 | 실행 버튼 유지·설정 미확정 | 추천 run 생성 안 됨 |
| PIT 후보 0 | 202 후 `LIVE_DATA_NOT_READY`/HOLD | 빈 결과를 성공 추천으로 표현하지 않음 | manual review=true |
| Worker timeout | DEGRADED 또는 HOLD | 해당 Worker failed/timeout 표시 | 다른 Worker 성공만으로 통과하지 않음 |
| Risk reject | reject/HOLD | 승인 버튼 숨김 또는 차단 사유 표시 | 주문/원장 변경 0 |
| QA grounded=false | inconclusive/escalate | 자동 승인 금지 | manual review=true |
| WebSocket 끊김 | REST status는 계속 조회 가능 | stale/offline 후 reconnect | event gap 재동기화 |
| WebSocket sequence gap | snapshot/event replay 제공 | gap 동안 이전 상태를 확정하지 않음 | 최신 status와 일치 |
| duplicate POST | 같은 idempotency 결과 또는 409 | 중복 실행 버튼 방지 | 두 run이 생기지 않음 |
| 브라우저 refresh | run 재조회 | run_id 기반 복구 | 이전 실행을 잃지 않음 |
| 다른 사용자 run 접근 | 401/403/404 정책 | 접근 오류 표시 | 결과 노출 없음 |
| Ollama 연결 실패 | DEGRADED/HOLD | 모델 실패를 성공으로 표시하지 않음 | auto approve 없음 |
| `APPROVE` | advisory status 변경만 | “주문 전송” 문구 금지 | broker/order/fill/ledger 변경 0 |

## 5. 부족한 부분을 채우는 우선순위 백로그

### P0 — 직접 E2E와 안전 경계에 필수

1. **공유 계약 단일화**
   - 대상: `apps/api/portfolio_schemas.py`, `ai-office/app/ops/portfolioClient.ts`, `PortfolioInterviewPanel.tsx`
   - 작업: nested response 모델·enum·error schema·skip reason을 고정하고 TypeScript client를 생성/검증한다.
   - 완료: 임의의 extra field, 누락 field, 잘못된 status가 프론트 state에 반영되지 않는다.

2. **Mandate–Recommendation binding**
   - 대상: `governance-api`, `apps/api/main.py`, `portfolio_runtime.py`, frontend clients
   - 작업: `case_id`, `mandate_version_id`, `policy_hash`를 start request/result에 필수화한다.
   - 완료: 승인되지 않은 version, 오래된 localStorage 정책, hash mismatch는 실행되지 않는다.

3. **run-scoped frontend state**
   - 대상: `PortfolioInterviewPanel.tsx`, `portfolioClient.ts`, `bffClient.tsx`
   - 작업: POST `run_id` 보존, GET status primary, WS는 notification+resync.
   - 완료: 두 탭/refresh/이전 실행의 결과가 섞이지 않는다.

4. **브라우저 E2E 테스트 도입**
   - 대상: `ai-office/e2e/`, CI workflow
   - 작업: Playwright와 deterministic fixture BFF를 연결한다.
   - 완료: 성공·PIT 부족·Risk reject·QA inconclusive·WS gap·duplicate·Governance down 시나리오를 CI에서 통과한다.

5. **인증·idempotency·영속 run store 설계/구현**
   - 대상: `apps/api/portfolio_runtime.py`, DB migration, API middleware
   - 완료: BFF restart와 duplicate request에도 실행·권한·감사 이력이 보존된다.

### P1 — 80% 운영 품질에 필요한 항목

6. **run event store와 replay cursor**
7. **health/live·health/ready와 dependency별 readiness**
8. **production CORS/auth/CSRF/rate limit/body size**
9. **Worker timeout/retry/heartbeat/cancellation**
10. **실제 PIT 국내 종목·시장 snapshot·portfolio strategy 적재와 freshness alarm**
11. **Risk/QA trace를 run/case timeline에 연결**
12. **Ollama 실제 호출 fixture와 model/prompt metadata 검증**
13. **`department_reports` typed summary: completed/skipped_safe/not_requested/failed/skip_reason**

### P2 — 실거래로 확장할 때만

14. OrderIntent → deterministic Risk Engine → OMS → Broker → Fill → Ledger의 별도 E2E.
15. kill switch, daily loss, position/sector/gross exposure, duplicate order, partial fill, reconciliation.
16. 외부 쓰기 권한 분리와 사용자 승인/운영자 승인/audit retention.
17. 실거래와 DEMO/PAPER projection을 서로 다른 endpoint·DB·credential로 분리.

## 6. 현재 데이터·환경 상태와 해석

최근 점검 결과 Supabase preflight는 연결·schema 관점에서는 PASS일 수 있지만 다음 데이터 상태는 별도다.

- `portfolio_catalog`/deployable strategy 후보가 없을 수 있다.
- `reference.instruments`의 PIT 국내 equity instrument가 없을 수 있다.
- `execution.market_snapshots`의 as-of snapshot이 없을 수 있다.
- research documents가 LIVE여도 가격·유니버스·시장 스냅샷의 대체 근거가 되지 않는다.
- 이 상태에서 `NO_VALID_PORTFOLIO_CANDIDATES`, `NO_PIT_DOMESTIC_EQUITY_INSTRUMENTS`, `NO_PIT_MARKET_SNAPSHOTS`와 `HOLD`가 나오는 것은 오류를 숨긴 것이 아니라 안전한 결과다.
- 다만 운영 화면에서 이 결과를 “분석 완료”로만 보여주면 안 되므로 `LIVE_DATA_NOT_READY`, counts, source, as-of, manual review를 함께 표시해야 한다.
- 기본 Worker runtime은 `deterministic_test`; 실제 Ollama는 `PORTFOLIO_WORKER_RUNTIME=ollama`, `OLLAMA_BASE_URL`, `OLLAMA_CHAT_MODEL` 설정과 readiness가 필요하다.

## 7. 검증 명령과 현재 결과

현재 문서 작성 시 다음 검증을 수행했다.

```bash
# 프론트
npm --prefix ai-office test
npm --prefix ai-office run lint

# Risk/QA 전용 환경의 API 계약·관측성
source /Users/baiohelseu/claude/bin/activate
python --version
command -v ruff
python -m pytest tests/api/test_portfolio_recommendation_bff.py \
  tests/api/test_pipeline_observability.py -q

# 배관 후보 정적 감사
python3 .agents/skills/evidence-wiring-audit/scripts/scan_wiring.py . --json

# diff 공백
git diff --check
```

확인된 결과:

- 프론트 build 및 rendered HTML test: 5건 통과.
- 프론트 ESLint: 통과.
- BFF portfolio recommendation/observability 테스트: 11건 통과.
- Evidence wiring scanner: 292개 후보 발견(`silent_except`, `unselected_columns`, `key_mismatch` 등이 포함). 이는 자동 후보 목록이지 모두 버그라는 뜻은 아니다. 예를 들어 QA의 `claim_checks[].result`는 실제 계약상 존재하지만 정적 scanner가 producer를 완전히 추론하지 못할 수 있다. 운영 승격 전에는 고위험 후보를 수동 triage해야 한다.
- 현재 저장소에는 실제 Chromium 기반 frontend↔BFF E2E suite가 없다. 이 항목은 “테스트 통과”로 간주하면 안 된다.

## 8. 최종 Definition of Done

다음 항목이 모두 참일 때만 “직접 프론트·백엔드 E2E 연결 완료”라고 표시한다.

```text
[ ] 브라우저가 생성한 trace_id가 Governance case와 recommendation run에 동일하게 연결된다.
[ ] 확정 mandate_version_id와 policy_hash가 Worker input/result/replay에 남는다.
[ ] 모든 BFF request/response/error가 공유 schema로 검증된다.
[ ] 프론트는 run_id-scoped status를 primary source로 사용한다.
[ ] WebSocket event는 sequence/cursor/replay로 누락을 복구한다.
[ ] process restart/multi-instance/duplicate request가 안전하게 처리된다.
[ ] Risk·QA 실패, PIT 부족, Ollama 실패는 자동 승인·주문 확대 없이 HOLD/DEGRADED 된다.
[ ] `SKIPPED_SAFE`, `NOT_REQUESTED`, `LIVE_DATA_NOT_READY`, `FAILED`가 구분된다.
[ ] 실제 브라우저 E2E가 CI에서 실패 매트릭스를 통과한다.
[ ] production auth/CORS/CSRF/rate limit/readiness가 적용된다.
[ ] 실제 데이터 fixture와 LIVE 데이터 readiness가 구분된다.
[ ] 현재 advisory 승인과 향후 Order/Fill/Ledger 쓰기가 별도 권한·계약으로 분리된다.
```

현재는 **안전한 advisory prototype의 내부 연결은 상당 부분 되어 있지만, 위 Definition of Done을 만족하는 직접 운영 E2E는 아직 완료되지 않았다.** 가장 먼저 할 일은 `mandate_version_id` binding, run-scoped frontend status, strict shared DTO, Playwright E2E, durable runtime/idempotency 순서다. 이 순서를 지키면 데이터가 부족하거나 서비스가 내려간 상황에서도 “정상 추천”으로 잘못 보이거나 자동 승인으로 넘어가는 문제를 구조적으로 차단할 수 있다.

