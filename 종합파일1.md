# 프론트엔드–백엔드 직접 E2E 연동 종합 감사·보완 명세 (개정판)

**작성일:** 2026-08-05 → **개정일:** 2026-08-06
**대상:** `/Users/baiohelseu/Desktop/Project/multi_agent` 현재 작업 트리 (`qa-department` 브랜치, 미커밋 변경분 포함)
**개정 사유:** 원본 `종합파일.md` 작성 이후, 같은 작업 트리에서 P0 항목 다수를 닫는 미커밋 구현이 실제로 진행되고 있는 것을 확인했다. 이 개정판은 주장이 아니라 **코드 diff 대조 + 실제 테스트 재실행 결과**로 각 항목의 상태를 다시 매긴 것이다.

> **감사 방식에 대한 경고.** 이 문서를 작성하는 동안 `apps/api/main.py`, `apps/api/portfolio_schemas.py` 등이 세션 도중에도 계속 바뀌었다 (예: `PORTFOLIO_REQUIRE_MANDATE_BINDING` 추가, nested response의 `dict[str, Any]` → 타입 모델 전환이 감사 도중 커밋 없이 반영됨). 즉 **다른 세션/프로세스가 이 작업 트리를 동시에 수정하고 있다.** 아래 내용은 검증 명령을 마지막으로 재실행한 시점(섹션 7)의 스냅샷이며, 이후 다시 바뀌었을 수 있다. 병렬 작업이 계획된 것이 아니라면 동시 편집으로 인한 충돌·유실 위험을 먼저 확인해야 한다.

> 이 문서는 “절대 문제가 생기지 않는다”는 보증서가 아니다. 대신 문제가 발생해도 주문 확대나 자동 승인이 일어나지 않고 `HOLD/REJECT/DENY/ESCALATE`로 멈추며 원인을 재현할 수 있게 하는 **fail-closed acceptance 기준**을 정의한다. 현재 구현은 주문·원장 변경이 없는 advisory prototype이므로, 아래 기준을 충족하기 전에는 실거래 시스템으로 표현하거나 운영 승격하지 않는다.

## 0. 결론 요약

흐름 자체는 원본 문서와 동일하다.

```text
브라우저 입력
  → ai-office/app/ops/portfolioClient.ts
  → FastAPI BFF /ui/portfolio-recommendations
  → apps/api/portfolio_runtime.py (in-process run, 선택적 SQLite 영속)
  → orchestration/workflows/portfolio_recommendation.py
  → 부서별 LangGraph Worker fan-out/fan-in
  → Supabase PIT 데이터·Risk·QA 게이트
  → 결과/이벤트를 PortfolioRuntime에 투영
  → GET /ui/portfolio-recommendations/{run_id} (신규 primary) + GET /ui/snapshot + WS /ws/operations
  → BffProvider → PortfolioInterviewPanel
```

원본 문서 대비 **실제로 달라진 지점**만 먼저 요약한다.

| 영역 | 08-05 판단 | 08-06 재검증 결과 |
|---|---|---|
| 실행별 상태 조회 (F-02) | 미구현, 전역 snapshot만 사용 | **구현됨.** `PortfolioInterviewPanel.tsx`가 `startPortfolioRecommendation` 응답의 `run_id`를 `portfolioRun` state에 저장하고 `fetchPortfolioRecommendation(run_id)`를 1.5초 간격으로 poll해 종결 상태까지 primary source로 쓴다. |
| Mandate–추천 실행 binding (F-03/F-05) | 완전한 단일 계약 아님 | **서버 강제로 승격됨.** `PORTFOLIO_REQUIRE_MANDATE_BINDING` 기본값이 `true`로 바뀌어 `mandate_version_id`/`policy_hash` 없는 시작 요청은 422가 된다. 프론트도 두 값이 없으면 시작을 막는다. 다만 `policy_hash`는 여전히 브라우저가 만든 값을 서버가 그대로 신뢰하며, Governance의 실제 활성 정책과 서버 측에서 대조 검증하지는 않는다. |
| BFF 응답 런타임 검증 (F-01) | unsafe cast | **부분 구현.** `portfolioClient.ts`에 `requireObject/requireString/requireArray/requireBoolean` 수동 파서가 추가돼 universes/start/status/approval 응답을 검증하고 실패 시 예외로 던진다. 코드 생성이나 zod는 아니지만 이전의 무검증 cast보다 실질적으로 안전하다. |
| Nested response 타입 (B-03) | `dict[str, Any]` | **타입 모델로 전환됨.** `PortfolioRiskGate`, `PortfolioQaGate`, `PortfolioWorkerReport`, `PortfolioDepartmentReport`, `PortfolioDataContext`, `PortfolioPipelineEvent`, `PortfolioReplayMetadata` Pydantic 모델이 신설돼 `PortfolioRecommendationResult`에 적용됐다. |
| Run 영속성 (B-01) | process-local 메모리 전용 | **선택적 영속화.** `apps/api/portfolio_store.py`의 `PortfolioRuntimeStore`가 `PORTFOLIO_RUNTIME_STORE_PATH` 설정 시 SQLite에 마지막 run을 저장하고, 프로세스 재시작 후에도 종결 상태를 복구한다. 다중 동시 run·다중 인스턴스는 여전히 미지원(진행 중이던 run은 복구가 아니라 안전하게 `ERROR`로 전환). |
| Idempotency·소유권 (B-02) | 없음 | **부분 구현.** `Idempotency-Key`/`X-User-Id` 헤더가 추가돼 in-memory 중복 방지와 owner mismatch 403이 동작한다. 단, idempotency map은 메모리 전용(재시작 시 유실)이고 owner 검증은 `PORTFOLIO_AUTH_REQUIRED`가 기본 OFF라 지금은 강제되지 않는다. |
| Readiness (B-07 일부) | `/health`만 존재 | **`/health/ready` 신설.** bff/governance/supabase/ollama/pipeline/runtime_store/mandate_binding을 개별 노출하고 secret은 반환하지 않는다(테스트로 검증됨). CORS/CSRF/rate limit은 여전히 개발용 그대로다. |
| 브라우저 E2E (F-04) | 전무 | **CI에 연결됨, 그러나 커버리지는 작다.** `ai-office/e2e/portfolio-advisory.spec.mjs` 2건 + `.github/workflows/ai-office-e2e.yml`이 실제로 Chromium에서 통과한다. 다만 두 테스트 모두 "BFF offline → fail-closed 렌더링" 경로만 검증하며, 원본 문서가 요구한 성공 경로·WS gap·PIT 부족·409/422/503·Worker DEGRADED 시나리오는 아직 없다. |
| **(신규) F-09 회귀** | — | **`PORTFOLIO_REQUIRE_MANDATE_BINDING` 기본값 전환이 기존 기능을 깼다.** `ai-office/app/page.tsx:1258-1270`의 `restartAnalysis()`가 호출하는 `startSavedPortfolioRecommendation()`(`portfolioClient.ts:155`)은 `mandate_version_id`/`policy_hash`를 채우지 않는다. 이 경로는 이제 항상 422를 받는다. 실패가 조용히 넘어가지 않고 에러 메시지로 표시되긴 하므로 안전 방향으로는 깨졌지만, "저장된 Mandate로 분석 재시작" 기능 자체가 동작하지 않는다. |

원본 문서가 지적한 항목 중 **바뀌지 않은 것**: B-04(데이터 readiness 별도 gate), B-05(run-scoped WS/SSE replay — 여전히 전역 `/ws/operations` + REST poll), B-06(Worker timeout/heartbeat/cancellation), B-07 후반부(production CORS/CSRF/rate limit), B-08(공통 error schema `error_code/retryable`), B-09(`mindset`/`experience` 아직 plain `str`, Literal 아님), B-10(Ollama live vs deterministic_test 계약 비교), F-06(localStorage 초안의 서버측 hash 재검증), F-07(프론트가 `/health/ready`를 아직 소비하지 않음 — 백엔드만 개별 dependency를 노출한다), F-08(단위·상태 표시).

실행 가능성 추정치를 재산정하면:

| 범위 | 08-05 추정 | 08-06 재검증 | 근거 |
|---|---:|---:|---|
| 프론트 입력·BFF 호출 | 60% | **75%** | run-scoped state, 런타임 검증, mandate binding guard가 실제로 들어옴. localStorage 신뢰 경계(F-06)와 `/health/ready` 미소비(F-07)는 남음. |
| BFF·런타임·LangGraph | 65% | **72%** | nested schema 타입화, 선택적 영속화, idempotency 골격은 생겼으나 다중 인스턴스·영속 idempotency·run-scoped event replay는 미해결. |
| Risk·QA 내부 Worker/게이트 | 80~85% / 75~80% | 변경 없음(미검증) | 이번 세션에서 관련 파일 변경 없음. |
| 프론트↔백엔드 공동 E2E | 40~50% | **50~55%** | 브라우저 E2E가 CI에 실제로 붙었지만 커버리지가 offline 케이스 2건뿐이라 실패 매트릭스 대부분은 여전히 미검증. |
| 실거래 운영 준비도 | 20% 미만 | 변경 없음 | 범위 밖. |

## 1. 현재 E2E 계약과 소유권

이 절은 원본과 동일하다 — 이번 diff는 계층 소유권이나 금지 책임을 바꾸지 않았다. 원본 1.1, 1.2절 내용이 그대로 유효하다. 다만 1.2절의 "advisory recommendation까지만 다룬다"는 제약은 이번 diff에서 `input_hash`/`trace_id`/`case_id`/`mandate_version_id`/`policy_hash`가 start/status 응답에 실제로 노출되면서 재현성 측면은 강화됐다.

## 2. 프론트엔드

대상 파일: `ai-office/app/ops/PortfolioInterviewPanel.tsx`, `ai-office/app/ops/portfolioClient.ts`, `ai-office/app/ops/bffClient.tsx`, `ai-office/app/ops/readModel.ts`, `ai-office/app/ops/governanceClient.ts`, `ai-office/app/page.tsx`.

### 2.1 이번 diff로 새로 구현된 것

1. `portfolioClient.ts`에 `requireObject/requireArray/requireString/requireBoolean`과 `parseRecommendationResult`가 추가돼 universes/start/status/approval 응답의 필수 필드·타입을 런타임에 검증한다. 위반 시 `Error`를 던지고, 상위에서 `submitError`/`connectionError`로 노출돼 화면이 `undefined`를 정상처럼 보여주지 않는다.
2. `fetchPortfolioRecommendation(runId, ownerId)`가 신설돼 `run_id`, `trace_id`, `case_id`, `mandate_version_id`, `policy_hash`, `input_hash`, `result`, `error`를 타입이 있는 `PortfolioRunStatus`로 반환한다.
3. `PortfolioInterviewPanel`이 `startRecommendation()` 이후 `run_id`를 `portfolioRun` state에 보관하고, 종결 상태(`COMPLETED/DEGRADED/HOLD/FAILED/CANCELLED`)까지 1.5초 간격으로 재조회한다(`PortfolioInterviewPanel.tsx:535-552`). 화면에 `run_id`/`trace_id`/`case_id`/`mandate_version_id`/`policy_hash`/`input_hash`/`phase`/`error`를 그대로 표시한다.
4. `recommendationInput()`이 `trace_id`(세션당 1회 `crypto.randomUUID()`), `case_id`, `mandate_version_id`, `policy_hash`를 추천 요청에 포함시킨다. `startRecommendation()`은 `mandate_version_id`/`policy_hash` 둘 중 하나라도 없으면 요청 자체를 막는다(`PortfolioInterviewPanel.tsx:628-636`).
5. `error` 텍스트에 `role="alert"`가 붙어 스크린리더 접근성이 개선됐다(`--ink-faint` 대비값 조정, 아이콘 `aria-hidden` 포함 — WCAG AA 대응, 이번 리뷰 범위와는 별개지만 회귀는 아니다).

### 2.2 부족한 부분과 위험 (갱신)

#### F-01. 컴파일 타입과 런타임 계약 — **P0 → 부분 해결, 잔여 위험 P1**

수동 파서가 4개 엔드포인트의 최상위 필드는 검증하지만, `apps/api/portfolio_schemas.py`가 새로 만든 `PortfolioRiskGate`/`PortfolioWorkerReport`/`PortfolioDepartmentReport`/`PortfolioPipelineEvent` 각각의 내부 필드까지는 프론트에서 검증하지 않는다(`parseRecommendationResult`는 해당 키가 object/array라는 것만 확인한다). 백엔드가 이미 강한 타입을 갖췄으므로, 이제는 OpenAPI 스키마에서 TypeScript 타입을 생성해 이중 관리를 없애는 것이 남은 작업의 대부분이다.

**보완:** `apps/api`가 노출하는 OpenAPI에서 `openapi-typescript` 같은 생성기로 nested 타입까지 자동 반영한다. 수동 파서는 스키마 위반을 조기에 잡는 방어선으로 유지해도 된다.

#### F-02. 실행별 상태 vs 전역 snapshot — **P0 → 해결됨**

`run_id`가 저장되고 상태 조회의 primary source가 됐다. 다만 `/ui/snapshot`과 `portfolioRun` 두 source가 화면에 동시에 존재하는 지점(`runtime?.phase` 표시, `connectionError` 판정에 `runtime?.run_id` 사용)이 남아 있어, 두 source가 다른 run을 가리키는 경우의 UI 우선순위를 명시적으로 정리할 필요는 있다.

**잔여 작업(P1):** `/ui/snapshot`의 `operations.runtime`은 dashboard 표시 전용으로 명확히 분리하고, portfolio interview 화면의 모든 상태 문구는 `portfolioRun`만 참조하도록 정리한다.

#### F-03. Governance 정책과 추천 실행의 단일 version 묶음 — **P0 → 서버 강제 도입, 신뢰 경계는 미해결**

`mandate_version_id`/`policy_hash`가 이제 필수(`PORTFOLIO_REQUIRE_MANDATE_BINDING=true` 기본값)이고 pipeline input과 결과에 동일하게 남는다. 그러나 BFF는 `policy_hash`가 실제 Governance의 활성 정책과 일치하는지 자체 계산·대조하지 않는다 — 여전히 "브라우저가 보낸 값이 곧 정책"이다. 원본 문서가 지적한 "browser가 localStorage에 보관한 정책을 최종 정책으로 신뢰하지 않는다"는 목표는 필드가 필수화됐을 뿐 검증되지는 않았다.

**보완:** BFF가 시작 시점에 Governance API의 `GET /ui/mandates/{mandate_id}/current`를 조회해 클라이언트가 보낸 `policy_hash`와 서버가 계산한 hash를 대조하고 불일치 시 409/422로 거부한다.

#### F-04. 실제 브라우저 E2E — **P0 → CI 연결됨, 커버리지 협소**

`ai-office/e2e/portfolio-advisory.spec.mjs`(2 테스트)와 `.github/workflows/ai-office-e2e.yml`이 실제로 존재하고, 로컬 재실행 결과 2건 모두 통과했다(섹션 7). 두 테스트는 "BFF fetch를 강제로 abort시켜 fail-closed 오프라인 렌더링을 확인"하는 경로만 다룬다. 원본이 요구한 아래 경로는 여전히 비어 있다.

- universes 조회 → Mandate 입력 → Governance fast path → recommendation POST → run status polling (성공 경로 전체)
- WebSocket 이벤트 → `run_id` status 재조회 → 부서별 worker/event 표시
- WebSocket sequence gap → REST 재동기화
- PIT 데이터 없음 → HOLD 표시
- 422/409/503 및 worker DEGRADED 표시
- `APPROVE`가 주문·원장을 바꾸지 않는다는 표시(현재 테스트는 정적 문구 부재만 확인하고 실제 APPROVE 클릭 흐름은 다루지 않는다)

**보완:** fixture BFF(deterministic_test 모드) 앞에서 mock 없이 실제 `POST /ui/portfolio-recommendations`를 태우는 happy-path 테스트부터 추가한다.

#### F-05. 고급 입력의 저장과 실행 전달 불일치 — **P0 → 해결됨**

`startRecommendation()`이 `mandate_version_id`/`policy_hash` 없이는 실행을 막으므로, 화면에 보이는 한도가 곧 실행에 쓰이는 한도라는 보장이 생겼다(단, F-03 잔여 위험과 결합해 "그 값이 진짜 활성 정책과 같다"는 것까지 보장하지는 않는다).

#### F-06. localStorage 신뢰 경계 — **P1 → 미해결, 신규 회귀와 직결**

변경 없음. `startSavedPortfolioRecommendation()`(`ai-office/app/ops/portfolioClient.ts:155`)은 여전히 `mandate_version_id`/`policy_hash`를 전혀 채우지 않는다. F-03이 서버 강제로 바뀌면서 이 함수가 만드는 요청은 이제 **항상 422로 거부된다** — 아래 F-09 참고.

#### F-07. BFF/Governance/개별 dependency 상태 머신 — **P1 → 백엔드만 절반 구현**

백엔드에 `/health/ready`가 생겨 `bff/governance/supabase/ollama/pipeline/runtime_store/mandate_binding`을 개별 노출하지만(`apps/api/main.py`), 프론트 어디에서도 이 엔드포인트를 호출하지 않는다(`grep` 확인, 0건). 화면은 여전히 `bffClient`의 `connected` 단일 상태로 commandability를 판단한다.

**보완:** `bffClient.tsx`나 `PortfolioInterviewPanel`에 `/health/ready` polling을 추가하고, `commandability = bff connected ∧ governance READY ∧ mandate_binding READY`로 세분화한다.

#### F-08. 숫자·상태 의미 재해석 위험 — **P1 → 미해결**

변경 없음. `statusLabels.ts`에 `REVOKED`/`EXPIRED` 라벨만 추가됐고, `SKIPPED_SAFE`/`NOT_REQUESTED`/`LIVE_DATA_NOT_READY`/`BLOCKED`/`DEGRADED` 구분 표시나 Decimal 포맷터 도입은 아직이다.

#### F-09. (신규) `PORTFOLIO_REQUIRE_MANDATE_BINDING` 기본값 전환이 "저장된 Mandate로 재시작" 기능을 깼다 — **P0**

`ai-office/app/page.tsx:1258-1270`의 `restartAnalysis()` → `startSavedPortfolioRecommendation()` 경로는 localStorage draft에서 입력을 복원해 추천을 재시작하는 대시보드 복구 버튼이다. 이 함수는 `mandate_version_id`/`policy_hash`를 만들지도, 전달하지도 않는다. 백엔드가 이제 이 값들을 기본으로 요구하므로 버튼을 누르면 항상 `422 mandate_version_binding_required`가 뜬다. 자동 승인 방향으로 깨진 것은 아니므로 fail-closed 원칙은 지켜졌지만, 기능 자체가 죽어 있다.

**보완 두 가지 중 하나:**
1. `startSavedPortfolioRecommendation()`이 저장된 draft가 아니라 서버의 현재 활성 Mandate(`GET /ui/mandates/{mandate_id}/current`)를 다시 조회해 `mandate_version_id`/`policy_hash`를 채운 뒤 요청한다(F-06 보완과 동일한 방향).
2. 이 버튼을 "Mandate 설정 화면으로 이동해서 다시 제출"로 성격을 바꾼다.
1번이 원래 의도(저장된 설정으로 원클릭 재시작)에 더 가깝다.

### 2.3 프론트엔드 완료 수용 기준 (갱신)

- [x] 추천 시작 후 `run_id`가 state에 보존되고, 화면은 해당 run status를 primary source로 읽는다.
- [x] `mandate_version_id`, `policy_hash`, `input_hash`, `trace_id`, `case_id`가 실행 화면에서 조회 가능하다.
- [ ] 모든 BFF response가 nested 필드까지 런타임 schema parse를 통과한다 (최상위만 검증됨).
- [ ] `APPROVE`는 advisory result 승인으로만 명시되고 주문·Fill·Ledger UI로 이어지지 않는다 (미확인 — E2E에 해당 클릭 경로 없음).
- [ ] WebSocket gap/reconnect 후 REST 재동기화가 완료될 때까지 stale 상태를 표시한다 (원본과 동일한 미해결 상태로 재확인).
- [x] 실제 브라우저 E2E가 CI에서 수행된다 (커버리지는 offline 케이스뿐).
- [ ] 브라우저가 잘못된 응답·빈 후보·PIT 누락·Governance 장애를 성공으로 표시하지 않는다 (E2E 미검증).
- [ ] **(신규)** localStorage 기반 재시작 경로가 활성 Mandate binding과 일치한다 (F-09 — 현재 항상 실패).

## 3. 백엔드

대상 파일: `apps/api/main.py`, `apps/api/portfolio_runtime.py`, `apps/api/portfolio_store.py`(신규), `apps/api/portfolio_schemas.py`, `apps/api/portfolio_universe.py`, `orchestration/workflows/portfolio_recommendation.py`, `departments/03-risk`, `departments/06-ai-qa-audit`, Governance API.

### 3.1 이번 diff로 새로 구현된 것

1. `apps/api/portfolio_store.py` — `PORTFOLIO_RUNTIME_STORE_PATH` 설정 시에만 동작하는 SQLite 기반 `PortfolioRuntimeStore`. run 저장/최신 조회/`run_id` 조회를 제공하며 credential은 저장하지 않는다.
2. `PortfolioRuntime.__init__`이 store에서 마지막 run을 복구하고, 복구된 run이 `QUEUED/RUNNING`이면 즉시 `ERROR`로 전환한다(`portfolio_runtime.py:141-153`) — 재시작 중 유실된 실행을 "완료"로 오인하지 않게 하는 fail-closed 처리다.
3. `start()`에 `Idempotency-Key`(`user_id`, key) 기반 중복 방지가 추가돼 같은 키·다른 입력이면 `RuntimeError`(409), 같은 키·같은 입력이면 기존 `run_id`를 `idempotent_replay: true`로 재사용한다.
4. `PortfolioRecommendationRequest`에 `trace_id`/`case_id`/`mandate_version_id`/`policy_hash`가 추가됐고, `PORTFOLIO_REQUIRE_MANDATE_BINDING`(기본 `true`) 환경변수로 시작 시 이 값들을 강제할 수 있다. 강제가 꺼져 있어도 두 값이 "한쪽만 있는" 상태는 항상 422로 막는다.
5. `X-User-Id` 헤더와 `_require_portfolio_owner()`가 추가돼 `PORTFOLIO_AUTH_REQUIRED=true`일 때 미인증 시작을 401로, run 소유자가 다르면 403으로 막는다. 기본값은 `false`라 지금은 강제되지 않는다.
6. `GET /health/ready`가 신설돼 `bff/governance/supabase/ollama/pipeline/runtime_store/mandate_binding`을 개별 노출한다. secret을 절대 반환하지 않는 것을 테스트로 확인했다(섹션 7).
7. `apps/api/portfolio_schemas.py`에 `PortfolioRiskGate`, `PortfolioQaGate`, `PortfolioWorkerReport`, `PortfolioDepartmentReport`, `PortfolioDataContext`, `PortfolioPipelineEvent`, `PortfolioReplayMetadata` Pydantic 모델이 신설돼 `PortfolioRecommendationResult`의 거의 모든 nested 필드가 `dict[str, Any]`에서 벗어났다.

### 3.2 부족한 부분과 위험 (갱신)

#### B-01. `PortfolioRuntime`이 process-local이다 — **P0 → 부분 해결**

SQLite 영속화는 opt-in이고, 여전히 프로세스 메모리 안에 **동시에 하나의 run만** 유지한다(`self._job: dict[str, Any] | None`). 다중 worker/컨테이너 간 run 분산, run별 독립 실행(현재는 새 run 시작 시 이전 QUEUED/RUNNING run이 있으면 그냥 409)은 미해결이다. idempotency map(`self._idempotency`)도 메모리 전용이라 재시작하면 중복 방지 이력이 사라진다.

**보완:** 원본 문서의 execution store 필드 목록(run_id/trace_id/.../idempotency_key)을 SQLite 스키마에 그대로 반영하고, idempotency map도 같은 store에 영속화한다. 다중 동시 run 지원은 `_job` 단일 slot을 `dict[run_id, job]`으로 바꿔야 한다.

#### B-02. 시작 idempotency·사용자 권한·run 소유권 — **P0 → 부분 해결**

헤더·검증 로직은 생겼지만 기본값이 안전 방향이 아니다. `PORTFOLIO_AUTH_REQUIRED` 기본이 `false`이므로 **지금 기본 설정으로는 여전히 누구나 임의의 `run_id`를 조회할 수 있다**(테스트 `test_run_status_rejects_mismatched_owner_header`는 auth-required가 아닌데도 403을 기대하지 않고, `PORTFOLIO_AUTH_REQUIRED=true`로 patch한 테스트만 owner 검증을 확인한다 — 실제로는 `apps/api/main.py`의 `_require_portfolio_owner`가 owner_id가 존재할 때는 항상 mismatch를 막지만, owner_id 자체가 optional 헤더라 안 보내면 통과된다).

**보완:** production 배포 전 `PORTFOLIO_AUTH_REQUIRED=true`를 기본값으로 바꾸거나, 최소한 배포 설정에서 강제하는 것을 Definition of Done에 명시한다.

#### B-03. nested response가 `dict[str, Any]`다 — **P0 → 해결됨**

`RiskGate/QaGate/WorkerReport/DepartmentReport/DataContext/PipelineEvent/ReplayMetadata`가 모두 Pydantic 모델이 됐고, 기존 19개 테스트가 이 스키마로 통과한다(섹션 7). `suitability`/`task_plan`만 아직 `dict[str, object]`로 남아 있다 — 이 둘은 원본 문서에서도 우선순위가 상대적으로 낮았던 필드다.

**잔여 작업(P1):** `suitability`/`task_plan`도 타입화하면 B-03이 완전히 닫힌다.

#### B-04. Supabase 연결 성공과 추천 가능 데이터 준비는 별개다 — **P0, 변경 없음(미검증)**

이번 diff가 이 경로를 건드리지 않았다. 원본 문서의 지적이 그대로 유효하다고 가정한다(재확인 안 함).

#### B-05. WebSocket이 run-scoped event stream이 아니다 — **P1, 변경 없음**

`/ws/operations`는 여전히 global feed다. 프론트도 여전히 1.5초 REST poll로 대체하고 있다(F-02 구현 방식 자체가 이 gap을 우회한 것이지, 메운 것이 아니다). run-scoped `WS/SSE + sequence cursor`는 미구현.

#### B-06. Worker timeout·cancellation·heartbeat — **P1, 변경 없음**

미구현.

#### B-07. readiness·security 경계가 개발용이다 — **P0 → readiness만 부분 해결**

`/health/ready`는 생겼지만 CORS(`allow_origin_regex=r"^https?://(localhost|127\.0\.0\.1)(:\d+)?$"`)는 그대로다. CSRF·rate limit·body size limit·production origin allowlist는 미구현.

#### B-08. 공통 오류 스키마 — **P1, 변경 없음**

`error_code`/`retryable`/`trace_id`/`field_errors` 공통 스키마는 아직 없다. 새로 추가된 422 케이스들(`mandate_version_binding_required`, `mandate_policy_binding_required`, `portfolio_authentication_required`, `portfolio_recommendation_forbidden`)도 전부 `HTTPException(detail=문자열)`이라 프론트가 문자열 매칭 없이 재시도 가능 여부를 판단할 수 없다.

#### B-09. BFF 입력 enum과 Governance 정책 정합성 — **P1, 변경 없음**

`mindset`/`experience`는 여전히 plain `str`.

#### B-10. 실제 Ollama와 deterministic test 차이 — **P1 → 아주 일부만 진전**

`/health/ready`의 `ollama` 항목이 `PORTFOLIO_WORKER_RUNTIME`/`OLLAMA_BASE_URL` 설정 여부를 노출하게 됐다. 그러나 fixture-vs-live 계약 비교, model/prompt version replay metadata 검증은 여전히 없다.

#### B-11. 외부 쓰기 미연결 — 변경 없음, 여전히 정상 상태

### 3.3 백엔드 완료 수용 기준 (갱신)

- [x] `mandate_version_id`·`policy_hash`·`input_hash`·`trace_id`가 pipeline input과 결과에 동일하게 남는다.
- [x] 모든 nested result/event/report가 strict schema를 사용한다 (`suitability`/`task_plan` 제외).
- [ ] run이 API 프로세스 재시작과 다중 instance에서도 조회 가능하다 (단일 프로세스·단일 run 한정으로 부분 해결).
- [ ] idempotency와 인증된 user/run 소유권 검사가 기본값으로 강제된다 (현재 둘 다 opt-in, 기본 OFF).
- [ ] PIT readiness 실패가 성공·빈 추천으로 변환되지 않고 `LIVE_DATA_NOT_READY/HOLD`가 된다 (미검증, 변경 없음으로 가정).
- [ ] run-scoped event replay와 sequence gap 복구가 가능하다 (미구현).
- [ ] Worker timeout/retry/cancel/heartbeat와 terminal state가 구현된다 (미구현).
- [ ] production CORS/auth/CSRF/rate limit이 설정으로 강제된다 (readiness 노출만 됨, 강제는 없음).
- [x] 외부 쓰기는 별도 승인 Gate 없이 절대 발생하지 않는다 (`external_writes: false`가 `/health/ready`에도 노출됨, 테스트로 확인).

## 4. 백엔드와 프론트엔드가 함께 해야 할 내용

### 4.1 단일 E2E correlation 계약 — **P0 → 배선은 완료, 검증은 미완료**

원본이 요구한 필드(`trace_id`, `case_id`, `mandate_version_id`, `policy_hash`, `run_id`, `input_hash`)가 이제 start 요청 → runtime job → status 응답까지 실제로 흐른다(`portfolio_runtime.py`의 `_base_job`, `main.py`의 start/status 핸들러). `workflow_version`/`model_version`/`prompt_version`은 여전히 별도 필드로 노출되지 않는다 — `PortfolioReplayMetadata.schema_id`가 근접한 역할을 하지만 동일 개념은 아니다.

`event_id`/`sequence`는 `PortfolioPipelineEvent`에 존재하지만(`event_id`, 없다면 순번 필드는 확인 필요), run-scoped push 채널이 없어 "이벤트 중복·누락·재생 방지"라는 원래 목적은 아직 REST poll로 대체 중이다.

### 4.2 공유 상태 머신 — 변경 없음

원본 4.2절 표와 `department_reports` JSON 예시는 그대로 유효하다. `PortfolioDepartmentReport` Pydantic 모델이 `status`/`skip_reason`/`completed`/`skipped_safe`/`not_requested`/`failed_count`를 필드로 강제하게 되면서, 이 상태 머신을 어기는 응답은 이제 **Pydantic validation error로 500이 나거나 애초에 직렬화되지 않는다** — 계약이 코드로 고정됐다는 뜻이다.

### 4.3 API 계약 표 (갱신 — 추가/변경분만)

| Method | Path | 08-05 상태 | 08-06 상태 |
|---|---|---|---|
| GET | `/health/ready` | 없음 | **신설.** dependency별 readiness, secret 미노출 확인됨. 프론트 미소비. |
| POST | `/ui/portfolio-recommendations` | 인증·idempotency 없음 | `Idempotency-Key`/`X-User-Id` 헤더 지원(둘 다 opt-in 강제), `mandate_version_id`/`policy_hash` 기본 필수. |
| GET | `/ui/portfolio-recommendations/{run_id}` | 읽기 전용, 권한 미검사 | owner header 있으면 mismatch를 403으로 막음(단, 헤더 자체는 optional). |

나머지 행은 원본과 동일하다.

### 4.4 정식 E2E 시나리오 — 변경 없음

원본 4.4절의 15단계 시나리오는 여전히 목표 상태를 정확히 기술한다. 이번 diff는 6~9단계(추천 start에 mandate version 포함, `run_id`/`input_hash` 영속, run-scoped status 조회)를 실제로 구현했다는 점만 다르다. 10~12단계(Worker fan-out 실패 격리, HOLD/DEGRADED)는 검증 대상 파일이 바뀌지 않아 재확인하지 않았다.

### 4.5 E2E 실패 매트릭스 — 자동화 커버리지 갱신

| 시나리오 | 자동 테스트 존재 여부 (08-06 기준) |
|---|---|
| 잘못된 enum/비율(422) | BFF 단위 테스트로 커버(`test_portfolio_recommendation_bff.py`) |
| mandate binding 누락(422) | **신규 테스트로 커버**(`test_mandate_version_requires_policy_hash_binding`) |
| duplicate POST(idempotent replay) | **신규 테스트로 커버**(`test_idempotency_key_replays_same_advisory_run`) |
| 인증 필요 모드(401/403) | **신규 테스트로 커버**(`test_auth_required_mode_*`, `test_run_status_rejects_mismatched_owner_header`) |
| 프로세스 재시작 후 조회(durable store) | **신규 테스트로 커버**(`test_runtime_store_recovers_terminal_projection_after_new_instance`) |
| `/health/ready` secret 미노출 | **신규 테스트로 커버** |
| 브라우저 offline/fail-closed 렌더링 | **신규 Playwright 테스트로 커버**(2건) |
| WebSocket 끊김/gap, PIT 부족, Worker timeout, Risk reject, QA inconclusive, Governance 503, 다른 사용자 run 접근(비인증 기본값 상태), `APPROVE`의 no-write 확인 | **여전히 자동 테스트 없음** |

## 5. 우선순위 백로그 (갱신)

### 이미 닫힘 (검증됨, 커밋 대기 중인 미커밋 변경분)

- 공유 계약 단일화의 상당 부분(nested schema 타입화, 런타임 파서) — 원본 P0-1.
- Mandate–Recommendation binding의 서버 강제(값 검증까지는 아님) — 원본 P0-2.
- run-scoped frontend state — 원본 P0-3.
- 브라우저 E2E의 CI 배선 — 원본 P0-4 (커버리지는 미달).
- 영속 run store·idempotency의 골격 — 원본 P0-5 (기본값이 아직 안전 방향이 아님).

### P0 — 남은 필수 항목

1. **F-09 회귀 수정** — `startSavedPortfolioRecommendation()`이 활성 Mandate binding 정보를 채우도록 고친다. 지금 이 상태로 커밋되면 배포 즉시 기존 버튼이 깨진다.
2. **`policy_hash`의 서버 측 대조 검증** (F-03 잔여) — BFF가 Governance의 활성 정책 hash를 조회해 대조한다.
3. **`PORTFOLIO_AUTH_REQUIRED`/idempotency 영속화의 기본값 정책 결정** (B-01/B-02 잔여) — 지금처럼 opt-in이면 production Definition of Done을 통과할 수 없다. 최소한 배포 설정 문서에 강제를 명시한다.
4. **브라우저 E2E 실패 매트릭스 확장** (F-04 잔여) — 성공 경로, PIT 부족, 422/409/503, WS gap 순으로 추가.

### P1 — 80% 운영 품질

5. run-scoped event store/replay cursor (B-05, 변경 없음)
6. Worker timeout/retry/heartbeat/cancellation (B-06, 변경 없음)
7. production CORS/CSRF/rate limit/body size (B-07 후반부, 변경 없음)
8. 공통 error schema `error_code/retryable` (B-08, 변경 없음 — 새 422 케이스들도 이 문제를 그대로 물려받음)
9. `mindset`/`experience` Literal/Enum화 (B-09, 변경 없음)
10. Ollama live fixture 계약 비교 (B-10, 변경 없음)
11. `/health/ready`를 프론트에서 실제로 소비 (F-07 잔여)
12. `suitability`/`task_plan` 타입화 (B-03 잔여)

### P2 — 실거래 확장 시에만 (변경 없음)

원본 14~17번 항목 그대로.

## 6. 현재 데이터·환경 상태와 해석

원본 6절 내용을 그대로 유지한다 — 이번 diff가 `portfolio_universe.py`, PIT 데이터 적재 경로, Supabase 스키마를 건드리지 않았으므로 재검증하지 않았다.

## 7. 검증 명령과 결과 (2026-08-06 재실행)

```bash
# Risk/QA 전용 환경
source ~/claude/bin/activate
python --version   # 3.13.13
command -v ruff     # 있음

# BFF 계약·observability 테스트
python -m pytest tests/api/test_portfolio_recommendation_bff.py \
  tests/api/test_pipeline_observability.py -q

# 프론트 단위 테스트/린트
npm --prefix ai-office test
npm --prefix ai-office run lint

# 신규: 브라우저 E2E
npx playwright test   # ai-office/ 안에서 실행

# 배관 후보 정적 감사
python3 .agents/skills/evidence-wiring-audit/scripts/scan_wiring.py . --json

# diff 공백
git diff --check
```

확인된 결과:

- BFF portfolio recommendation/observability 테스트: **19건 통과** (원본 11건 → 증가, idempotency/auth/durable-store/health-ready 테스트가 새로 추가됨). 재검증 도중 한 번 6건이 일시적으로 실패했는데, 원인은 테스트 실행 시점에 `apps/api/main.py`가 다른 프로세스에 의해 편집 중이었기 때문으로 확인했다(재실행 시 다시 19건 전체 통과) — 커밋되지 않은 동시 편집이 진행 중이라는 근거다.
- 프론트 `node --test` 5건 통과, ESLint 통과 — 원본과 동일.
- **신규:** Playwright E2E 2건 통과(`ai-office/e2e/portfolio-advisory.spec.mjs`).
- Evidence wiring scanner: **292개 후보, 원본과 동일**(`silent_except` 106, `unselected_columns` 156, `key_mismatch` 19, `order_assumption` 8, `bounded_digit_regex` 3). 이번 diff 대상 파일들이 스캐너가 잡는 패턴 자체를 줄이지는 않았다는 뜻이다. 여전히 자동 후보 목록이며 전수 triage는 하지 않았다.
- `git diff --check`: `ai-office/package-lock.json`의 trailing whitespace만 검출됨(Playwright 의존성 추가로 인한 자동 생성 파일, 실질적 위험 없음).
- 저장소에는 실제 Chromium 기반 E2E가 이제 **존재**하지만, 커버리지가 offline 케이스 2건뿐이라 "성공 경로가 E2E로 검증됐다"고는 아직 말할 수 없다.

## 8. 최종 Definition of Done (갱신)

```text
[x] 확정 mandate_version_id와 policy_hash가 Worker input/result/replay에 남는다.
[x] 모든 BFF request/response의 최상위 필드가 공유 schema로 검증된다 (nested 필드 일부 예외: suitability/task_plan).
[x] 프론트는 run_id-scoped status를 primary source로 사용한다.
[x] 실제 브라우저 E2E가 CI에서 최소 1개 시나리오를 통과한다 (전체 실패 매트릭스는 아직 아님).
[x] 외부 쓰기는 별도 승인 Gate 없이 절대 발생하지 않는다 (/health/ready에서도 확인).
[ ] 브라우저가 생성한 trace_id가 Governance case와 recommendation run에 동일하게 연결된다 (필드는 흐르지만 서버 측 대조 검증 없음).
[ ] WebSocket event는 sequence/cursor/replay로 누락을 복구한다 (미구현, REST poll로 우회 중).
[ ] process restart/multi-instance/duplicate request가 기본 설정으로 안전하게 처리된다 (opt-in 상태, 기본값 미강제).
[ ] Risk·QA 실패, PIT 부족, Ollama 실패는 자동 승인·주문 확대 없이 HOLD/DEGRADED 된다 (미검증, 변경 없음으로 가정).
[ ] `SKIPPED_SAFE`, `NOT_REQUESTED`, `LIVE_DATA_NOT_READY`, `FAILED`가 화면에서 구분된다 (백엔드 스키마는 구분, 프론트 표시는 미해결).
[ ] 실제 브라우저 E2E가 CI에서 실패 매트릭스 전체를 통과한다.
[ ] production auth/CORS/CSRF/rate limit/readiness가 적용으로 강제된다 (readiness 노출만 있고 강제는 없음).
[ ] 실제 데이터 fixture와 LIVE 데이터 readiness가 구분된다 (미검증).
[ ] 현재 advisory 승인과 향후 Order/Fill/Ledger 쓰기가 별도 권한·계약으로 분리된다 (구조상 유지, 변경 없음).
```

**결론:** 원본 문서가 P0로 지목한 5개 항목 중 4개(F-02, F-03/F-05, F-04, B-01/B-02/B-03)가 이번 세션 중 실질적으로 진전됐고, 그중 F-02와 B-03은 사실상 완료로 볼 수 있다. 그러나 (1) 이 작업이 **아직 커밋되지 않았고 다른 프로세스가 동시에 계속 수정 중**이라는 점, (2) 새로 강제된 mandate binding이 기존 재시작 경로(F-09)를 깼다는 점, (3) `policy_hash` 검증과 인증 기본값이 여전히 "필드는 있지만 강제되지 않음" 상태라는 점 때문에 Definition of Done 통과를 선언할 수 없다. 다음 작업 순서를 권한다: **F-09 회귀 수정 → 현재 변경분 커밋 및 테스트 고정 → `policy_hash` 서버 측 대조 검증 → 인증/idempotency 기본값을 안전 방향으로 전환 → 브라우저 E2E 실패 매트릭스 확장.**
